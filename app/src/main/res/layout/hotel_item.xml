<?xml version="1.0" encoding="utf-8"?>
<!--
    Hotel list card — matches the dark-indigo card design in the reference image.
    Structure (top → bottom):
      1. Full-width hotel photo (centerCrop)
      2. Hotel name  (bold, white, serif)
      3. Rating row  (gold star + score)
      4. Bottom row  (price + "per Night" label | "Select" pill button)
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/hotel_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginTop="10dp"
    android:layout_marginBottom="6dp"
    app:cardBackgroundColor="#1A0B2E"
    app:cardCornerRadius="16dp"
    app:cardElevation="8dp"
    app:strokeColor="#2E1B4E"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- ── Hotel Image ── -->
        <ImageView
            android:id="@+id/hotel_image"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"
            android:contentDescription="@null" />

        <!-- ── Text + Button section ── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingTop="12dp"
            android:paddingBottom="14dp">

            <!-- Hotel Name -->
            <TextView
                android:id="@+id/hotel_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Hotel Name"
                android:textColor="#FFFFFF"
                android:textSize="17sp"
                android:textStyle="bold"
                android:fontFamily="serif"
                android:maxLines="2"
                android:ellipsize="end"
                android:lineSpacingMultiplier="1.15"
                android:layout_marginBottom="4dp" />

            <!-- Rating: gold star + score -->
            <TextView
                android:id="@+id/hotel_rating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="— ★"
                android:textColor="#F5A623"
                android:textSize="15sp"
                android:textStyle="bold"
                android:layout_marginBottom="12dp" />

            <!-- Bottom row: Price (left) | Select button (right) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Price block -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <!-- Price amount -->
                    <TextView
                        android:id="@+id/hotel_price"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="—"
                        android:textColor="#FFC107"
                        android:textSize="22sp"
                        android:textStyle="bold" />

                    <!-- "per Night" label -->
                    <TextView
                        android:id="@+id/hotel_price_label"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text=" per Night"
                        android:textColor="#B0A8C8"
                        android:textSize="12sp"
                        android:layout_marginStart="4dp"
                        android:paddingBottom="2dp" />

                </LinearLayout>

                <!-- Select Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_select"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:text="Select"
                    android:textColor="#1A0B2E"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:paddingStart="22dp"
                    android:paddingEnd="22dp"
                    android:backgroundTint="#FFC107"
                    app:cornerRadius="20dp"
                    app:rippleColor="#9B3EE8"
                    style="@style/Widget.MaterialComponents.Button" />

            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>